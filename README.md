# 🍞 نظام إدارة فرن "صمون بركات الرضا"

نظام ويب محلي متكامل لإدارة فرن الصمون مع واجهة عربية جميلة وسهلة الاستخدام.

## ✨ المميزات

- ✅ **واجهة عربية جميلة** - تصميم احترافي بألوان أسود وذهبي
- ✅ **ريسبونسف كامل** - يعمل على جميع الأجهزة (موبايل، تابلت، لابتوب)
- ✅ **بدون إنترنت** - يعمل محلياً 100% باستخدام localStorage
- ✅ **حسابات تلقائية** - كل الحسابات تتم تلقائياً
- ✅ **أنيميشن سلسة** - حركات انسيابية ومريحة للعين
- ✅ **سهل الاستخدام** - واجهة بسيطة ومباشرة

## 📂 محتويات المشروع

```
frn/
├── index.html      # الصفحة الرئيسية (Dashboard)
├── dough.html      # صفحة العجنات اليومية
├── flour.html      # صفحة كلفة الطحين
├── expenses.html   # صفحة المصاريف
├── profit.html     # صفحة حساب الربح النهائي
├── style.css       # ملف التنسيقات
├── db.js          # نظام قاعدة البيانات
└── README.md      # هذا الملف
```

## 🚀 طريقة الاستخدام

### 1️⃣ التشغيل المحلي

**الطريقة الأولى - فتح مباشر:**
- افتح ملف `index.html` في أي متصفح حديث (Chrome, Firefox, Edge)

**الطريقة الثانية - باستخدام Live Server (موصى بها):**

إذا كنت تستخدم VS Code:
1. نصب إضافة Live Server
2. اضغط كليك يمين على `index.html`
3. اختر "Open with Live Server"

### 2️⃣ استخدام النظام

#### الصفحة الرئيسية
- عرض سريع لإحصائيات اليوم
- إحصائيات حسب الفترة (يوم، أسبوع، شهر)

#### صفحة العجنات اليومية
1. اختر التاريخ
2. أدخل عدد عجنات الصباح
3. أدخل عدد عجنات المساء
4. **الربح يُحسب تلقائياً**: `(عدد العجنات × 10) ÷ 8`
5. اضغط حفظ

#### صفحة كلفة الطحين
1. اختر التاريخ
2. أدخل سعر الكيس
3. أدخل عدد الأكياس
4. **الكلفة تُحسب تلقائياً**: `السعر × العدد`
5. اضغط حفظ

#### صفحة المصاريف
1. اختر التاريخ
2. أدخل اسم المصروف (مثال: عمال، كهرباء، صيانة، وقود...)
3. أدخل المبلغ
4. اضغط حفظ
5. يمكنك إضافة عدة مصاريف لنفس اليوم

#### صفحة الربح النهائي
- عرض تقارير شاملة حسب الفترة
- حساب صافي الربح تلقائياً:
  ```
  صافي الربح = ربح العجنات - كلفة الطحين - المصاريف
  ```
- جدول مقارنة يومية
- إحصائيات إضافية (متوسط الربح، نسبة الربح، إلخ)

## 💾 حفظ البيانات

- **جميع البيانات تُحفظ محلياً** في متصفحك باستخدام localStorage
- البيانات **لا تُحذف** عند إغلاق المتصفح
- لحذف البيانات: افتح Console في المتصفح واكتب:
  ```javascript
  localStorage.clear()
  ```

## 📱 تحويله لتطبيق أندرويد

يمكن تحويل النظام لتطبيق أندرويد بسهولة:

### الطريقة 1 - Android Studio
1. أنشئ مشروع Android جديد
2. استخدم WebView
3. حمّل ملفات HTML, CSS, JS في مجلد assets

### الطريقة 2 - Apache Cordova
```bash
cordova create myApp com.example.barakatapp BarakatApp
cd myApp
# انسخ ملفات HTML/CSS/JS إلى www/
cordova platform add android
cordova build android
```

### الطريقة 3 - أدوات تحويل جاهزة
- استخدم أدوات مثل: Webintoapp, AppGeyser, Andromo

## 🎨 التخصيص

### تغيير الألوان
افتح ملف `style.css` وغيّر المتغيرات في `:root`:

```css
:root {
    --primary-black: #1a1a1a;
    --secondary-black: #2d2d2d;
    --gold: #FFD700;
    --dark-gold: #B8860B;
}
```

### تغيير صيغة حساب الربح
افتح ملف `db.js` وعدل في دالة `addDough`:

```javascript
morning_profit: (parseInt(data.morning_count) || 0) * 10 / 8,
// غيّر 10 و 8 حسب الصيغة المطلوبة
```

## 🔧 متطلبات التشغيل

- متصفح حديث يدعم:
  - HTML5
  - CSS3
  - JavaScript ES6
  - localStorage

- **لا يحتاج**:
  - ❌ قاعدة بيانات خارجية
  - ❌ إنترنت
  - ❌ سيرفر
  - ❌ تنصيبات معقدة

## 📊 معادلات الحساب

### ربح العجنات
```
ربح الصباح = (عدد عجنات الصباح × 10) ÷ 8
ربح المساء = (عدد عجنات المساء × 10) ÷ 8
الربح الإجمالي = ربح الصباح + ربح المساء
```

### كلفة الطحين
```
الكلفة = سعر الكيس × عدد الأكياس
```

### صافي الربح
```
صافي الربح = ربح العجنات - كلفة الطحين - كلفة العمال - المصاريف الإضافية
```

## 🎯 نصائح الاستخدام

1. ✅ سجّل البيانات يومياً للحصول على إحصائيات دقيقة
2. ✅ استخدم نفس المتصفح دائماً للحفاظ على البيانات
3. ✅ عمل نسخة احتياطية دورية (export البيانات)
4. ✅ لا تستخدم وضع التصفح الخفي (Incognito)

## ⚠️ تنبيهات مهمة

- البيانات محفوظة في المتصفح فقط
- مسح بيانات المتصفح = فقدان البيانات
- استخدم جهاز واحد فقط لإدخال البيانات
- لا يمكن التزامن بين أجهزة متعددة (محلي فقط)

## 🆘 حل المشاكل

### المشكلة: البيانات لا تُحفظ
- تأكد أنك لا تستخدم وضع التصفح الخفي
- تأكد أن المتصفح يدعم localStorage
- تحقق من إعدادات الخصوصية في المتصفح

### المشكلة: الصفحة لا تعمل
- تأكد من فتح ملف index.html
- تحقق أن جميع الملفات في نفس المجلد
- استخدم متصفح حديث

### المشكلة: التصميم لا يظهر بشكل صحيح
- تأكد أن ملف style.css موجود
- تحقق من اتصال الإنترنت (لتحميل خط Cairo)
- جرّب تحديث الصفحة (Ctrl+F5)

## 📞 الدعم والتطوير

لأي استفسارات أو اقتراحات:
- يمكن تطوير النظام وإضافة مميزات جديدة
- يمكن تحويله لتطبيق أندرويد كامل
- يمكن إضافة نظام نسخ احتياطي

## 📝 الترخيص

هذا المشروع مجاني ومفتوح المصدر - استخدمه كما تشاء!

---

**تم التطوير بـ ❤️ لفرن صمون بركات الرضا**

🍞 بالتوفيق والرزق الوفير! 🍞

