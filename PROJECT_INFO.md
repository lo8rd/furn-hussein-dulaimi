# 📁 بنية المشروع - نظام إدارة فرن صمون بركات الرضا

## 📄 الملفات الرئيسية

### 🌐 ملفات HTML (الصفحات)

| الملف | الوصف | الوظيفة الرئيسية |
|------|-------|------------------|
| `START_HERE.html` | **صفحة البداية** | نقطة الانطلاق للمستخدمين الجدد - تعريف بالنظام |
| `index.html` | **الصفحة الرئيسية** | Dashboard يعرض الإحصائيات السريعة |
| `dough.html` | **صفحة العجنات** | تسجيل وإدارة العجنات اليومية |
| `flour.html` | **صفحة الطحين** | تسجيل كلفة وكميات الطحين |
| `expenses.html` | **صفحة المصاريف** | تسجيل المصاريف والعمال |
| `profit.html` | **صفحة الربح النهائي** | تقارير وإحصائيات شاملة |
| `test_data.html` | **بيانات تجريبية** | إضافة بيانات للاختبار (اختياري) |

### 🎨 ملفات التصميم والبرمجة

| الملف | الوصف | التفاصيل |
|------|-------|----------|
| `style.css` | **ملف التنسيقات** | - تصميم ريسبونسف<br>- ألوان: أسود، ذهبي، أبيض<br>- أنيميشن CSS<br>- خط Cairo العربي |
| `db.js` | **قاعدة البيانات** | - localStorage للتخزين<br>- CRUD operations<br>- دوال الإحصائيات |

### 📖 ملفات التوثيق

| الملف | الوصف |
|------|-------|
| `README.md` | دليل شامل بالإنجليزية والعربية |
| `دليل_الاستخدام.txt` | دليل سريع بالعربية |
| `PROJECT_INFO.md` | هذا الملف - بنية المشروع |

---

## 🔧 البنية التقنية

### قاعدة البيانات (localStorage)

#### 1️⃣ جدول `dough` (العجنات)
```javascript
{
  id: timestamp,
  date: "YYYY-MM-DD",
  morning_count: number,
  evening_count: number,
  morning_profit: number (calculated),
  evening_profit: number (calculated),
  total_profit: number (calculated)
}
```

#### 2️⃣ جدول `flour` (الطحين)
```javascript
{
  id: timestamp,
  date: "YYYY-MM-DD",
  bag_price: number,
  bag_count: number,
  total_cost: number (calculated)
}
```

#### 3️⃣ جدول `expenses` (المصاريف)
```javascript
{
  id: timestamp,
  date: "YYYY-MM-DD",
  expense_name: string,  // اسم المصروف (مثال: عمال، كهرباء، صيانة)
  amount: number         // المبلغ
}
```
**ملاحظة:** يمكن إضافة عدة مصاريف لنفس التاريخ

---

## 🧮 المعادلات الحسابية

### ربح العجنات
```javascript
morning_profit = (morning_count × 10) ÷ 8
evening_profit = (evening_count × 10) ÷ 8
total_profit = morning_profit + evening_profit
```

### كلفة الطحين
```javascript
total_cost = bag_price × bag_count
```

### إجمالي المصاريف
```javascript
total_expenses = sum(all expenses amounts for the period)
// يتم جمع جميع المصاريف في الفترة المحددة
```

### صافي الربح
```javascript
net_profit = total_dough_profit - total_flour_cost - total_expenses
```

---

## 🎨 نظام الألوان

```css
--primary-black: #1a1a1a     /* الخلفية الرئيسية */
--secondary-black: #2d2d2d   /* الخلفية الثانوية */
--gold: #FFD700              /* اللون الذهبي (لون الصمون) */
--dark-gold: #B8860B         /* الذهبي الداكن */
--white: #ffffff             /* النصوص */
--light-gray: #f5f5f5        /* النصوص الثانوية */
```

---

## 📱 Responsive Breakpoints

```css
/* Desktop: افتراضي */
/* Tablet: max-width: 768px */
/* Mobile: max-width: 480px */
```

---

## ⚡ مميزات الأداء

- ✅ **تحميل سريع** - لا توجد مكتبات خارجية ثقيلة
- ✅ **تخزين محلي** - استجابة فورية
- ✅ **حسابات تلقائية** - JavaScript نقي
- ✅ **أنيميشن CSS** - سلسة وخفيفة

---

## 🔐 الأمان والخصوصية

- ✅ **100% محلي** - لا يتم إرسال أي بيانات للإنترنت
- ✅ **لا توجد Cookies تتبعية**
- ✅ **البيانات في جهازك فقط**
- ✅ **عدم الاتصال بأي API خارجي**

---

## 🚀 خطوات التشغيل

### الطريقة 1: فتح مباشر
```
1. انقر مزدوجاً على START_HERE.html
2. سيفتح في المتصفح الافتراضي
3. ابدأ الاستخدام!
```

### الطريقة 2: Live Server (VS Code)
```
1. نصب إضافة Live Server
2. كليك يمين على index.html
3. اختر "Open with Live Server"
```

### الطريقة 3: Python Server
```bash
cd C:\Users\Monta\Downloads\frn
python -m http.server 8000
# افتح http://localhost:8000 في المتصفح
```

---

## 🔄 سير العمل اليومي

```
صباحاً:
  dough.html → تسجيل عجنات الصباح

مساءً:
  dough.html → تسجيل عجنات المساء

عند شراء الطحين:
  flour.html → تسجيل الكلفة

نهاية اليوم:
  expenses.html → تسجيل المصاريف
  
مراجعة دورية:
  index.html → الإحصائيات السريعة
  profit.html → التقارير المفصلة
```

---

## 📊 الإحصائيات المتاحة

### 1️⃣ حسب الفترة
- اليوم
- هذا الأسبوع
- هذا الشهر

### 2️⃣ أنواع التقارير
- عدد العجنات
- إجمالي الربح
- كلفة الطحين
- المصاريف
- صافي الربح
- متوسط الربح اليومي
- نسبة الربح
- جدول المقارنة اليومية

---

## 🛠️ التخصيص

### تغيير معادلة حساب الربح
افتح `db.js` → دالة `addDough()` → غيّر الصيغة:
```javascript
morning_profit: (morning_count × NEW_FORMULA),
```

### تغيير الألوان
افتح `style.css` → `:root` → غيّر المتغيرات

### إضافة حقول جديدة
1. أضف في النموذج (HTML)
2. أضف في قاعدة البيانات (db.js)
3. أضف في الحسابات (JavaScript)

---

## 📱 تحويل لتطبيق Android

### باستخدام Android Studio
```
1. أنشئ مشروع Android جديد
2. أضف WebView في MainActivity
3. انسخ الملفات إلى assets/
4. حمّل index.html في WebView
```

### باستخدام Cordova
```bash
cordova create BarakatApp
cd BarakatApp
# انسخ الملفات إلى www/
cordova platform add android
cordova build android
```

---

## 🐛 حل المشاكل الشائعة

### البيانات لا تُحفظ
```
❌ المشكلة: التصفح الخفي (Incognito)
✅ الحل: استخدم وضع التصفح العادي
```

### التصميم لا يظهر
```
❌ المشكلة: ملف style.css غير موجود
✅ الحل: تأكد من وجود جميع الملفات في نفس المجلد
```

### الخطوط لا تظهر بشكل صحيح
```
❌ المشكلة: عدم الاتصال بالإنترنت عند أول تحميل
✅ الحل: اتصل بالإنترنت مرة واحدة لتحميل خط Cairo
```

---

## 📦 حجم المشروع

```
db.js           ~5 KB
style.css       ~10 KB
index.html      ~4 KB
dough.html      ~6 KB
flour.html      ~6 KB
expenses.html   ~6 KB
profit.html     ~8 KB
test_data.html  ~4 KB
START_HERE.html ~8 KB

الإجمالي: ~57 KB (خفيف جداً!)
```

---

## 🎯 ميزات مستقبلية محتملة

- [ ] تصدير البيانات إلى Excel/PDF
- [ ] نسخ احتياطي تلقائي
- [ ] رسوم بيانية (Charts)
- [ ] نظام المستخدمين
- [ ] تطبيق Android/iOS
- [ ] إشعارات تذكير
- [ ] دعم أكثر من فرن
- [ ] مزامنة سحابية (اختياري)

---

## 💡 نصائح للتطوير

### إضافة صفحة جديدة
```html
1. انسخ أي صفحة موجودة كقالب
2. غيّر المحتوى حسب الحاجة
3. أضف رابط في النافيجيشن
4. استخدم نفس الكلاسات CSS
```

### إضافة ميزة جديدة في قاعدة البيانات
```javascript
// في db.js
addNewTable(data) {
    const newTable = this.getAllNewTable();
    const newEntry = { /* ... */ };
    newTable.push(newEntry);
    localStorage.setItem('newTable', JSON.stringify(newTable));
}
```

---

## 📞 دعم

للحصول على مساعدة:
1. اقرأ `README.md`
2. راجع `دليل_الاستخدام.txt`
3. جرّب `test_data.html` للتأكد من عمل النظام

---

## 📜 الترخيص

مشروع مفتوح المصدر - استخدمه بحرية!

---

**بالتوفيق والرزق الوفير!** 🍞✨

*تم التطوير بـ ❤️ لفرن صمون بركات الرضا*

